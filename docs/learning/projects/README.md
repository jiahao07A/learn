# 项目目录说明

1. 本目录存放阶段项目文档与里程碑证据。
2. 每个项目使用 `project-slug` 命名子目录：`docs/learning/projects/<project-slug>/`。
3. 每个项目目录必须包含：
   - `README.md`
   - `milestones.md`
   - `evidence.md`
4. 项目代码应存放于 `workspace/projects/<project-slug>/`，并在 `evidence.md` 记录会话关联证据。
